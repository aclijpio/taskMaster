spring:
  application:
    name: task-management
  profiles:
    active: default
  cloud:
    config:
      uri: http://localhost:8061
keycloak:
  realm: spmia-realm
  auth-server-url: http://keycloak:8080/auth
  ssl-required: external
  resource: jouh
  credentials:
    secret: BNsptMNLIbLIPZTtL1UR42ZHtOXQIXW1
  use-resource-role-mappings: true
  bearer-only: true


